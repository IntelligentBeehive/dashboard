const styles = theme => ({
    videoPlayerContainer: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        margin: '0 auto',
    }
});

export default styles;
